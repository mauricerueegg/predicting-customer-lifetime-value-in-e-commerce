# Predicting Customer Lifetime Value in E-Commerce

You are given the code for generating a dataset describing customer behaviour in an e-commerce environment, including purchase history, engagement metrics, and demographic attributes. The dataset allows you to model customer lifetime value (CLV). However, relevant information is distributed across different variables and may require aggregation or transformation. Your task is to build regression models that predict customer lifetime value and to interpret the drivers behind these predictions.

Objective
- Engineer meaningful features from behavioural data
- Train at least two regression models
- Evaluate and compare model performance
- Use SHAP values to explain model predictions

Hints
- Consider aggregating behavioural metrics into meaningful indicators
- Reflect on how past behaviour may relate to future value
- Be aware of potential skewed distributions in monetary variables
- Compare models with different assumptions (e.g. linear vs. non-linear)
- Use SHAP to understand both global and local feature effects

## Project Requirements

Individual Project Selection & Assignment
This section provides general information as well as an overview of all available individual project topics. Each student will be assigned one topic and complete it individually.

Project Assignment
Students have the opportunity to indicate their interests in the following focus areas:
Focus A) "Structured Python"
Focus B) "Data transformation & EDA"
Focus C) "Data organization & storage"
Focus D) "Data validation & statistics"
Focus E) "Supervised Learning: Regression and XAI"
Focus F) "Supervised Learning: Klassifikation and XAI"
Afterwards, students will be assigned to a topic, taking their preferences into account as much as possible

Project Deliverables
Each project must be submitted as one Jupyter Notebook (.ipynb) and the corresponding raw (original after initial creation) dataset (.csv).
The notebook must be:
fully executable (run from top to bottom without errors)
clearly structured
reproducible
The raw dataset (CSV)
The dataset must correspond to the original output generated by the provided code

Standardised Notebook Structure (Mandatory)
All notebooks must follow the structure below:

Problem Framing: Clearly define the task and objective of the project.
Data Understanding: Describe the dataset, its structure, and key characteristics.
Method / Approach: Explain your chosen approach and justify your decisions.
Implementation: Provide clean, structured, commented and readable Python code. Ensure that your implementation follows basic software hygiene principles (e.g. meaningful naming, modularisation, validation, and traceability).
Note: An object-oriented design (e.g. use of classes and inheritance) is required for topics in Focus A (Structured Python). For all other topics, a well-structured scripting approach (e.g. functions, clear workflow, and modular code) is sufficient.
Results, Validation & Robustness: Present the results (e.g. tables, plots, metrics), demonstrate that your results are reliable and critically assess potential sources of error or bias.
Interpretation & Critical Reflection: Interpret the results and discuss limitations and potential risks.
AI Usage Documentation: See details below.

AI Usage Documentation (Mandatory)
All students must explicitly document how AI tools (e.g. ChatGPT, Copilot) were used. Include the following subsections:

Role of AI: What tasks was AI used for (e.g. coding, debugging, ideation)?
Prompting Strategy: Provide at least 2–3 example prompts and explain how they were developed.
Orchestration & Timing: At which stages of the project was AI used?
Validation of AI Outputs: How did you verify correctness and reliability of AI-generated results?
Limitations of AI: Where did AI produce incorrect or misleading outputs?
Reflection: What did you learn about using AI in data science workflows?

Important Note on AI Usage
AI is considered a supporting tool, not a source of truth.
All outputs must be critically evaluated and validated.

---

## Implementation Reference

### Dataset

- **File:** `01_data/topic_E2_customer_lifetime_value_raw.csv`
- **Rows:** 1100 | **Columns:** 11 (10 features + 1 target)
- **Seed:** `np.random.seed(808)` — ensures reproducibility
- **Target:** `customer_lifetime_value_chf` (continuous, min 20 CHF)

| Column | Type | Description |
|--------|------|-------------|
| customer_tenure_months | int | Months since first purchase (1–60) |
| purchase_count_last_12m | int | Number of purchases in last 12 months (0–45) |
| avg_order_value_chf | float | Average order value in CHF (10–400) |
| return_rate | float | Proportion of returned orders (0–1) |
| website_visits_last_3m | int | Website visits in last 3 months |
| email_click_rate | float | Email engagement rate (0–1) |
| customer_segment | category | "new", "occasional", "loyal", "premium" |
| preferred_device | category | "mobile", "desktop", "tablet" |
| support_tickets_last_12m | int | Support tickets filed in last 12 months (0–12) |
| discount_usage_rate | float | Rate of discount usage (0–1) |
| **customer_lifetime_value_chf** | **float** | **TARGET — CLV in CHF** |

### Required Libraries

```
pandas, numpy
matplotlib, seaborn
scikit-learn (LinearRegression, RandomForestRegressor, StandardScaler, train_test_split, cross_val_score, mean_squared_error, mean_absolute_error, r2_score)
xgboost (XGBRegressor)
shap
```

Install missing packages in a cell at the top of the notebook if needed:
```python
# !pip install xgboost shap
```

### Models

Three regression models must be trained and compared:

1. **Linear Regression** (sklearn) — linear baseline; use scaled features
2. **Random Forest Regressor** (sklearn) — ensemble, handles non-linearity
3. **XGBoost Regressor** (xgboost) — gradient boosting, best for complex interactions

### Evaluation Metrics

- **RMSE** — root mean squared error (primary metric)
- **MAE** — mean absolute error (robust to outliers)
- **R²** — coefficient of determination (explained variance)
- **5-fold cross-validation** on training set for all models

### SHAP Analysis

- `shap.LinearExplainer` for Linear Regression
- `shap.TreeExplainer` for Random Forest and XGBoost
- Global: `shap.summary_plot()` (beeswarm + bar)
- Local: `shap.waterfall_plot()` for individual predictions
- Interaction: `shap.dependence_plot()` for top features

### Code Style

- Use **functions** for all reusable logic (no copy-paste code)
- Add docstrings to every function
- Use **meaningful variable names**
- Use `random_state=29` for all stochastic operations
- Comment non-obvious logic inline
- Section headers as Markdown cells; code in code cells

### Documentation

All design decisions are documented in `/docs/`:
- `00_project_overview.md` — project context and deliverables
- `01_data_description.md` — dataset schema and generation logic
- `02_feature_engineering.md` — feature engineering decisions
- `03_model_selection.md` — model choice rationale
- `04_evaluation_strategy.md` — metrics and validation approach
- `05_shap_methodology.md` — SHAP theory and interpretation
- `06_ai_usage_template.md` — template for AI usage documentation
